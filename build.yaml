# Kept for Supervisors that still read build.yaml (pre-2026.04 local builds).
# Supervisor 2026.04+ uses the Dockerfile FROM default when this file is absent.
build_from:
  aarch64: ghcr.io/home-assistant/aarch64-base:3.21
  amd64: ghcr.io/home-assistant/amd64-base:3.21
