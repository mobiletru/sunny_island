# Changelog

## 2.2.17

- **Fix:** plant UI no longer asks for a long-lived HA token. Ingress
  `/ha-ws` proxies Home Assistant WebSocket with `SUPERVISOR_TOKEN`
  (`homeassistant_api: true`). Sidebar app connects live without paste.

## 2.2.16
